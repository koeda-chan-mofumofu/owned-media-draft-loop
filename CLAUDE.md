# SEOオウンドメディア コンテンツ運用

## 目的
検索意図に応える読者に向け、確認済みの一次情報または自分の経験・専門知識を根拠に、
公開前の記事下書きを作成する。

このリポジトリは `sns-content-loop`（X/note向け短中文コンテンツ）とは別運用とする。
両者の間で自動連携は行わない。承認済みの記事を他方の素材として使う場合も、
必ず人間が内容を確認してから手動でコピーする。

## 実行順序
1. `source/inbox/` の素材（メモ・経験・調査結果・キーワード案）から、
   記事の主張・読者の検索意図・根拠を整理する。
2. キーワード調査・競合記事の構成調査は、`source/briefs/` に企画書としてまとめる。
   Web検索など通常のリサーチの範囲で行い、有料API（Ahrefs等）との自動連携は行わない。
3. 記事の見出し構成（H2/H3）を設計し、`source/briefs/` に記録する。
4. `seo-article` スキルを使い、`output/draft/` に記事下書きを保存する。
5. スキルのセルフチェックを通ったものだけ `output/review/` に移す。
6. 人間の明示的な承認なしに `output/publish-ready/` へ移さない。
   WordPress等への実際の投稿・公開操作は人間が行う。

## 絶対ルール
- 外部サービスへの投稿、送信、削除、課金、権限変更を実行しない。
  WordPressへの自動投稿、既存公開記事の自動編集（内部リンク追加等）、記事の自動削除は行わない。
- APIキー・ログイン情報・アクセストークンをリポジトリに保存しない。
  Ahrefs / X API / YouTube Data API / Google Indexing API等、外部サービスとの
  自動連携（cron等による定期実行を含む）は導入しない。
- 根拠を確認できない情報には `[要確認]` を付ける。数値・固有名詞・日付・引用は、
  `source/approved/` に根拠がなければ推測で補わない。
- アフィリエイトリンクは実URLを書かず `[要挿入:アフィリエイトリンク（商品名）]` と記載する。
  ASPへの登録・提携申請・リンク発行は人間が行う。
- 1回の処理は素材1件・最大30分で停止する。
- 同じ修正を2回受けた場合、`memory/quality-rules.md` への追記案を作る。
  ただし、人間の承認なしに運用ルールを確定変更しない。
- 完了時は、入力、出力、検証結果、未解決事項を作業ログへ残す。
